# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CHAT ME' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CHAT ME
   pod 'CLTypingLabel'
   pod 'Firebase/Auth'
   pod 'Firebase/Firestore'   
   pod 'Firebase/Core'
   pod 'Firebase/Storage'

   
  target 'CHAT METests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CHAT MEUITests' do
    # Pods for testing
  end

end
